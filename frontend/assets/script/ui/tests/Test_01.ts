import { UIDelegate } from "../../core/ui/UIDelegate";

const {ccclass, property, menu} = cc._decorator;

@ccclass()
@menu("ui/test/Tests_01")
export class Test_01 extends UIDelegate {

    protected start(): void {
    }
};
